

type Margin = {
    top: number; bottom: number; left: number; right: number
}

interface ChartSize {
    innerWidth: number, innerHeight: number, outerWidth: number, outerHeight: number
}


export type { ChartSize, Margin };

